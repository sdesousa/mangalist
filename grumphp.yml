parameters:
    git_dir: .
    bin_dir: './vendor/bin'
    hooks_dir: ~
    hooks_preset: local
    ascii:
        failed: grumphp-grumpy.txt
        succeeded: grumphp-happy.txt
    tasks:
        phpcs:
            standard: PSR12

        phpmd:
            whitelist_patterns:
                - 'src'
            ruleset:
                - 'phpmd.xml'
